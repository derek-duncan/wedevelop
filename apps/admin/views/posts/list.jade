extends ../templates/default.jade

block body
  .nav
    a.nav-item(href='/admin/posts') List
    a.nav-item(href='/admin/posts/add') Add
  .mainContainer
    h2 All posts
    each post in posts
      h4
        a(href='/admin/posts/#{post.machine_name}')= post.title
