.dropCap {
  float: left;
  display: inline-block;
  padding: 1rem 3rem 1rem 0;
  font: 600 6rem/1 var(--sansFont);
  text-transform: uppercase;
}

p {
  padding: 0 0 2rem;

  font-size: 1.6rem;
  line-height: 1.5;
}
