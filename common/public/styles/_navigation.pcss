.nav {
  max-width: calc(var(--width-nav--max) - 2rem);
  margin: 0 auto;
  padding: 2rem;
}

.nav-logo {
  margin: 0;
  font-size: 1.6rem;
  font-weight: 400;
  color: var(--color-text);
  opacity: 0;
  animation: fadeIn 250ms forwards;
}
